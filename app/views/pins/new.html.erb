<body class="background">

<div class="container">
<%= form_for @pin, html: {multipart: true} do |f| %>
  <% if @pin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pin.errors.count, "error") %> prohibited this pin from being saved:</h2>

      <ul>
      <% @pin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form-group" style="margin-top: 25%;">
<h1 style="color: #fff; text-align: center;">Upload Photo</h1>

    <%= f.label :image %>
    <%= f.file_field :image, class: "form-control new-post-input-field" %>
  </div>

    <%= f.submit "Upload", class: "landing-button btn-block"%>

<% end %>
<div style="margin-top: 10px;">
<%= link_to raw('<span class="glyphicon glyphicon-arrow-left back-arrow"></span><span class="back-arrow-text text-white position-left">Back</span>'), {:controller => 'users', :action => 'photos'}, style: "margin-top: 25%; font-size:15px; "%>
</div>

</div>
</body>
